const mockValidProduct = {
    id: 256,
    name: 'string',
    price: 111,
    userId: 256
};

const product = {
    name: mockValidProduct.name,
    price: mockValidProduct.price,
    userId:mockValidProduct.userId,
  };

export default {
    mockValidProduct,
    product,
}